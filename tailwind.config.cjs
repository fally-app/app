const config = {
	mode: 'jit',
	purge: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		fontFamily: {
			display: ['Poppins', 'sans-serif']
		},
		extend: {}
	},

	plugins: []
};

module.exports = config;
